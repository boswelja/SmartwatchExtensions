ext.versions = [
		versionCode: getDate(),
		versionCodeDebug: getTime(),
		versionName: "1.7.0.1",
		compileSdk: 28,
		gradlePlugin: '3.4.0-alpha10',
		wearableLib: "2.4.0",
		playServicesWearable: "16.0.1",
		packageName: "com.boswelja.devicemanager",
		buildTools: "28.0.3"
]

static def getDate() {
	return new Date().format('yyyyMMdd').toInteger()
}

static def getTime() {
	return new Date().format('ddhhmm').toInteger()
}